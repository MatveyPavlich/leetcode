# Python
```
import pandas as pd

def find_customers(visits: pd.DataFrame, transactions: pd.DataFrame) -> pd.DataFrame:
    df = visits.merge(
        transactions,
        how="left",
        on="visit_id"
    )
    df = df[df["transaction_id"].isna()]
    df_agg = (
        df
        .groupby("customer_id")
        .agg(
            count_no_trans=("visit_id", "count")
        )
        .reset_index()
    )

    return df_agg[["customer_id", "count_no_trans"]]
```

# Pandas
```
import pandas as pd

def find_classes(courses: pd.DataFrame) -> pd.DataFrame:
    df = (
        courses
        .groupby("class")
        .agg(n_student=("student", "count"))
        .query("n_student >= 5")
        .reset_index()
    )
    return df[["class"]]
```

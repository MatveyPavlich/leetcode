# Pandas
```
import pandas as pd

def find_employees(employees: pd.DataFrame) -> pd.DataFrame:
    df = employees[employees["manager_id"].notna()].merge(
        employees[["employee_id", "name"]],
        left_on="manager_id",
        right_on="employee_id",
        how="left",
        suffixes=("", "_manager")
    )
    df["non_left_manager"] = np.where(df["name_manager"].notna(), 1, 0)
    out = df[
        (df["non_left_manager"] == 0) &
        (df["salary"] < 30000)
    ][["employee_id"]]
    out.sort_values(by="employee_id", ascending=True, inplace=True)

    return out
```
